Program = StmtList ;

Block = "{" StmtList "}" ;
StmtList = { Stmt ";" } ;

Stmt = IfStmt | WhileStmt | Command ;

IfStmt = "if" Command Block [ "else" ( IfStmt | Block ) ] ;

WhileStmt = "while" Command Block ;

Command = CommandName { Argument } ;
CommandName = Word ;
Argument = Word ;

Word = /* TBD */ ;
